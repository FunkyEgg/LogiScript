package repl;

import constants.Types;

function launchRepl(info: InfoJson) {
    Console.logPrefix = '';
    Console.log('Using LogiScript: ${info.version} on stream ${info.stream} ');

    var exit: Bool = false;

    while (!exit) {
        
    }
}